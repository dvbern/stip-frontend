// TODO write test
/*
import { TestBed } from '@angular/core/testing';

import { SharedUtilValidatorDate } from './shared-util-validator-date';

describe('SharedUtilValidatorDateService', () => {
  let service: SharedUtilValidatorDate;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(SharedUtilValidatorDate);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/
