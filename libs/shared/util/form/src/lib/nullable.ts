export const unsetString: string | undefined = undefined;
/**
 * Used for boolean radio buttons that should have undefined as initial value
 * but are marked as required so the form value should be required
 */
export const optionalRequiredBoolean: boolean = undefined as unknown as boolean;
